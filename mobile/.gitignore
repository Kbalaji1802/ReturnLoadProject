# Flutter / Dart build + tooling artifacts.
.dart_tool/
.packages
.pub-cache/
build/
.flutter-plugins
.flutter-plugins-dependencies

# IDE
.idea/
*.iml

# Platform ephemeral files (regenerated by `flutter create .`)
android/.gradle/
ios/Flutter/.last_build_id
