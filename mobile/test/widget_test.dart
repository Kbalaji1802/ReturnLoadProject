// The driver/load-owner app is exercised end-to-end against the API (see the backend
// integration tests and the manual click-through), not via widget tests. This placeholder
// keeps the test target valid; replace with real widget tests as screens stabilise.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('smoke: test target compiles', () {
    expect(1 + 1, equals(2));
  });
}
