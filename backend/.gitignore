# .NET build artifacts — never committed (regenerated by `dotnet build`).
bin/
obj/
[Bb]in/
[Oo]bj/

# User-specific / IDE
*.user
*.suo
*.userprefs
.vs/

# Test results & coverage
[Tt]est[Rr]esults/
*.trx
coverage/
*.coverage
*.cobertura.xml

# Local runtime config (never commit real secrets — 01_PROJECT_RULES.md §1)
appsettings.*.local.json
